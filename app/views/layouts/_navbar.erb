<nav class='navbar fixed-top navbar-dark navbar-bjc'>
  <span class='navbar-brand'>
    <a href='/' aria-label="Home">
      <%= image_pack_tag('bjc.png', width: 30, height: 30, 'aria-hidden': 'true') %>
    </a>
    <%- if content_for?(:title) %>
      <span class="navbar-title"><%= yield(:title) %></span>
    <% else %>
      <span class="navbar-title">BJC Teachers Portal</span>
    <%- end %>
  </span>
  <div class="trapezoid"></div>
  <div>
    <ul class='nav justify-content-end'>
      <% if @is_admin %>
        <% admin_nav_links.each do |name, url|  %>
          <li class='nav-item'>
            <%= link_to name, url, class: "nav-link" %>
          </li>
        <% end %>
      <% end %>
      <li class='nav-item'>
        <%= link_to "Pages", pages_path, class: "nav-link" %>
      </li>
      <% if current_user %>
        <li class='nav-item'>
          <%= link_to "Update Info", edit_teacher_path(current_user.id), class: "nav-link" %>
        </li>
        <li class='nav-item'>
          <a class='nav-link' href='/logout' data-method='delete'>Logout</a>
        </li>
      <% else %>
        <li class='nav-item'>
          <a class='nav-link' href='/login'>Log In</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
